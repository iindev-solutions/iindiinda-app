/**
 * Конфигурация API
 *
 * Для переключения между моком и реальным API:
 *
 * 1. Мок режим (разработка фронтенда):
 *    export const USE_MOCK_API = true
 *
 * 2. Реальный API (подключение к Laravel):
 *    export const USE_MOCK_API = false
 *
 * После переключения перезапустите dev сервер
 */

// ⚡️ ПЕРЕКЛЮЧАТЕЛЬ API ⚡️
export const USE_MOCK_API = true

// Дополнительные настройки мока
export const MOCK_CONFIG = {
	// Вероятность ошибки (0-1), 0.1 = 10% шанс ошибки
	errorRate: 0.1,

	// Базовая задержка ответа (мс)
	baseDelay: 300,

	// Максимальная дополнительная задержка (мс)
	maxExtraDelay: 500,

	// Автоматический прием заказа водителем (для демо)
	autoAcceptDelay: 5000
}
